
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        
        /* Hero Section */
        .hero-section {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            padding: 4rem 0;
            text-align: center;
        }

        .hero-content {
            max-width: 800px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        .hero-title {
            font-size: 3rem;
            margin-bottom: 1rem;
            font-weight: 700;
        }

        .hero-subtitle {
            font-size: 1.2rem;
            opacity: 0.9;
            margin-bottom: 2rem;
        }

        /* Main Content */
        .main-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 3rem 2rem;
        }

        .section {
            margin-bottom: 3rem;
        }

        .section-title {
            font-size: 2rem;
            margin-bottom: 1rem;
            color: #2c3e50;
            border-bottom: 3px solid #3498db;
            padding-bottom: 0.5rem;
            display: inline-block;
        }

        .dark-mode .section-title {
            color: #e0e0e0;
        }

        /* Features Grid */
        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin-top: 2rem;
        }

        .feature-card {
            background: white;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
            border-left: 4px solid #3498db;
        }

        .feature-card:hover {
            transform: translateY(-5px);
        }

        .feature-icon {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            display: block;
        }

        .feature-title {
            font-size: 1.3rem;
            margin-bottom: 1rem;
            color: #2c3e50;
        }

        .dark-mode .feature-title {
            color: #e0e0e0;
        }

        /* Stats Section */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 2rem;
            margin-top: 2rem;
        }

        .stats-card {
            background: white;
            padding: 2rem;
            border-radius: 10px;
            text-align: center;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            border-top: 4px solid #e74c3c;
        }

        .stats-number {
            font-size: 2.5rem;
            font-weight: bold;
            color: #e74c3c;
            display: block;
        }

        .stats-label {
            font-size: 1rem;
            color: #7f8c8d;
            margin-top: 0.5rem;
        }

        /* Team Section */
        .team-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-top: 2rem;
        }

        .team-card {
            background: white;
            padding: 2rem;
            border-radius: 10px;
            text-align: center;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }
        .team-card:hover{
          border-color: #667eea;
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            transform: translateY(-5px);
        }

        .action-icon {
            font-size: 2rem;
            margin-bottom: 1rem;
            display: block;
        }  
        

        .dark-mode .team-card {
            background: #f8f0f0;
        }

        .team-card:hover {
            transform: translateY(-5px);
        }

        .team-avatar {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            background: linear-gradient(135deg, #667eea, #764ba2);
            margin: 0 auto 1rem;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            color: white;
        }

        .team-name {
            font-size: 1.2rem;
            font-weight: bold;
            margin-bottom: 0.5rem;
            color: #2c3e50;
        }

        .dark-mode .team-name {
            color: #e0e0e0;
        }

        .team-role {
            color: #7f8c8d;
            font-size: 0.9rem;
        }

        /* Contact Section */
        .contact-info {
            background: white;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            margin-top: 2rem;
        }

        .dark-mode .contact-info {
            background: #edeaeaf2;
        }

        .contact-item {
            display: flex;
            align-items: center;
            margin-bottom: 1rem;
            gap: 1rem;
        }

        .contact-icon {
            font-size: 1.5rem;
            color: #3498db;
            width: 30px;
        }



        

        /* Responsive Design */
        @media (max-width: 768px) {
            .nav-links {
                display: none;
            }

            .mobile-menu {
                display: flex;
            }

            .hero h1 {
                font-size: 2rem;
            }

            .cta-buttons {
                flex-direction: column;
                align-items: center;
            }

            .nav-container {
                padding: 0 1rem;
            }

            .main-content {
                padding: 2rem 1rem;
            }
        }
    </style>
</head>
<body>
  
<%- include('Component/Navigation.ejs') %>
    <!-- Hero Section -->
    <section class="hero-section">
        <div class="hero-content">
            <h1 class="hero-title">About Our Library</h1>
            <p class="hero-subtitle">Empowering knowledge through innovative library management solutions</p>
        </div>
    </section>

    <!-- Main Content -->
    <main class="main-content">
        <!-- Mission Section -->
        <section class="section">
            <h2 class="section-title">Our Mission</h2>
            <p>Our Library Management System is designed to revolutionize how libraries operate in the digital age. We believe that access to knowledge should be seamless, efficient, and accessible to everyone. Our platform combines cutting-edge technology with user-friendly design to create an experience that benefits both librarians and library users.</p>
        </section>

        <!-- Features Section -->
        <section class="section">
            <h2 class="section-title">Key Features</h2>
            <div class="features-grid">
                <div class="feature-card">
                    <span class="feature-icon">🔍</span>
                    <h3 class="feature-title">Advanced Search</h3>
                    <p>Powerful search capabilities allow users to find books by title, author, genre, ISBN, or keywords with lightning-fast results.</p>
                </div>
                <div class="feature-card">
                    <span class="feature-icon">👥</span>
                    <h3 class="feature-title">User Management</h3>
                    <p>Comprehensive member management system with detailed profiles, borrowing history, and personalized recommendations.</p>
                </div>
                <div class="feature-card">
                    <span class="feature-icon">📊</span>
                    <h3 class="feature-title">Analytics & Reports</h3>
                    <p>Detailed analytics and reporting tools help librarians make data-driven decisions and track library performance.</p>
                </div>
                <div class="feature-card">
                    <span class="feature-icon">📱</span>
                    <h3 class="feature-title">Mobile Friendly</h3>
                    <p>Responsive design ensures seamless access across all devices - desktop, tablet, and mobile.</p>
                </div>
                <div class="feature-card">
                    <span class="feature-icon">🔔</span>
                    <h3 class="feature-title">Smart Notifications</h3>
                    <p>Automated reminders for due dates, reservations, and new arrivals keep users informed and engaged.</p>
                </div>
                <div class="feature-card">
                    <span class="feature-icon">🌐</span>
                    <h3 class="feature-title">Digital Resources</h3>
                    <p>Seamless integration with digital collections, e-books, and online databases for comprehensive resource access.</p>
                </div>
            </div>
        </section>

        <!-- Statistics Section -->
        <section class="section">
            <h2 class="section-title">Library Statistics</h2>
            <div class="stats-grid">
                <div class="stats-card">
                    <span class="stats-number">50,000+</span>
                    <span class="stats-label">Books in Collection</span>
                </div>
                <div class="stats-card">
                    <span class="stats-number">5,000+</span>
                    <span class="stats-label">Active Members</span>
                </div>
                <div class="stats-card">
                    <span class="stats-number">2,500+</span>
                    <span class="stats-label">Monthly Transactions</span>
                </div>
                <div class="stats-card">
                    <span class="stats-number">99.8%</span>
                    <span class="stats-label">System Uptime</span>
                </div>
            </div>
        </section>

        <!-- Team Section -->
        <section class="section">
            <h2 class="section-title">Our Team</h2>
            <div class="team-grid">
                <div class="team-card">
                    <div class="team-avatar">👨‍💼</div>
                    <h3 class="team-name">Dr. Sarah Johnson</h3>
                    <p class="team-role">Head Librarian</p>
                </div>
                <div class="team-card">
                    <div class="team-avatar">👩‍💻</div>
                    <h3 class="team-name">Mike Chen</h3>
                    <p class="team-role">Systems Administrator</p>
                </div>
                <div class="team-card">
                    <div class="team-avatar">👨‍🎓</div>
                    <h3 class="team-name">Alex Rodriguez</h3>
                    <p class="team-role">Information Specialist</p>
                </div>
                <div class="team-card">
                    <div class="team-avatar">👩‍🏫</div>
                    <h3 class="team-name">Emma Thompson</h3>
                    <p class="team-role">User Experience Manager</p>
                </div>
            </div>
        </section>

        <!-- Contact Section -->
        <section class="section">
            <h2 class="section-title">Contact Information</h2>
            <div class="contact-info">
                <div class="contact-item">
                    <span class="contact-icon">📍</span>
                    <span>123 Knowledge Street, Education City, EC 12345</span>
                </div>
                <div class="contact-item">
                    <span class="contact-icon">📞</span>
                    <span>+1 (555) 123-4567</span>
                </div>
                <div class="contact-item">
                    <span class="contact-icon">✉️</span>
                    <span>info@librarysystem.edu</span>
                </div>
                <div class="contact-item">
                    <span class="contact-icon">🕒</span>
                    <span>Mon-Fri: 8:00 AM - 8:00 PM | Sat-Sun: 10:00 AM - 6:00 PM</span>
                </div>
            </div>
        </section>
    </main>

    <script>
        // Dark mode toggle functionality
        function toggleDarkMode() {
            document.body.classList.toggle('dark-mode');
            const isDarkMode = document.body.classList.contains('dark-mode');
            localStorage.setItem('darkMode', isDarkMode);
            
            const button = document.querySelector('.dark-mode-toggle');
            button.textContent = isDarkMode ? '☀️ Light Mode' : '🌓 Dark Mode';
        }

        // Mobile menu toggle
        function toggleMobileMenu() {
            const navMenu = document.getElementById('navMenu');
            navMenu.classList.toggle('active');
        }

        // Load dark mode preference
        document.addEventListener('DOMContentLoaded', function() {
            const isDarkMode = localStorage.getItem('darkMode') === 'true';
            if (isDarkMode) {
                document.body.classList.add('dark-mode');
                const button = document.querySelector('.dark-mode-toggle');
                button.textContent = '☀️ Light Mode';
            }
        });

        // Close mobile menu when clicking outside
        document.addEventListener('click', function(event) {
            const navMenu = document.getElementById('navMenu');
            const toggleButton = document.querySelector('.mobile-menu-toggle');
            
            if (!navMenu.contains(event.target) && !toggleButton.contains(event.target)) {
                navMenu.classList.remove('active');
            }
        });

        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Add animation to cards on scroll
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver(function(entries) {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        // Observe all cards for animation
        document.addEventListener('DOMContentLoaded', function() {
            const cards = document.querySelectorAll('.feature-card, .stats-card, .team-card');
            cards.forEach(card => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(20px)';
                card.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
                observer.observe(card);
            });
        });
    </script>
